import React from "react";
import { View } from "react-native/types";


type taskListProps = {
  tasks: taskItem[];
}

const TaskList = (props: taskListProps) => {
  return(
    <View>
      
    </View>
  );
}
